language: node_js
node_js:
 — "7"
script:
 — echo "Deploying..."
install:
 — npm install && npm install -g firebase-tools
after_success:
 — firebase deploy --project $FIREBASE_ID --token $FIREBASE_TOKEN
